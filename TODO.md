# TODO

- When two generations are going on at the same time, `generating = false` will be called when any of the generations finish, meaning one of them is still generating while `generating = false`